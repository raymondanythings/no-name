import ReactIcon from 'assets/icons/react.png'
import ReactBadge from 'assets/icons/react_badge.png'
import Redux from 'assets/icons/redux.png'
import Vue from 'assets/icons/vue.png'
const IMAGE_LIST = [
	{ key: 'react', src: ReactIcon },
	{ key: 'badge', src: ReactBadge },
	{ key: 'redux', src: Redux },
	{ key: 'vue', src: Vue }
]
interface useRotateProps {
	id: string
}

function useRotate({ id }: useRotateProps) {
	return
}

export { useRotate }
